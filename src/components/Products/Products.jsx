import React from "react";
import FeatureProducts from "./MainProducts";

const Products = ({ products }) => {
  return (
    <>
      <FeatureProducts products={products} />
    </>
  );
};

export default Products;
